# Git
.git
.gitignore

# Node modules (these will be installed within the container)
node_modules

# Environment files (these should be injected at runtime, not baked into the image)
.env
.env.*
!.env.example # Keep example if you want to show it

# Test reports
coverage/
junit.xml

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Editor and OS files
.DS_Store
Thumbs.db
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
